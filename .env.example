GITHUB_USERNAME=DhanushSantosh
GITHUB_TOKEN=
GITHUB_WEBHOOK_SECRET=
CRON_SECRET=

# Google Tag Manager container ID (public by design, not a secret — it's
# visible in every page's source regardless). Unset by default so GTM never
# fires in local dev/CI; set it in Vercel's Production environment to enable
# it there.
NEXT_PUBLIC_GTM_ID=
